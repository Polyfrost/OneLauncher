// import process from 'node:process';
// import pathe from 'pathe';

// export async function tauriUpdateKey(): Promise<string | undefined> {
// 	if (process.env.TAURI_SIGNING_PRIVATE_KEY)
// 		return;

// 	const privateKeyPath = pathe.join();
// }
